{
  "name": "aws-lambda-bootstrap",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run clean && npm run build && node build/index.js",
    "build": "babel src --out-dir build && cp -r src/config build",
    "clean": "rm -rf build",
    "test": "mocha --compilers js:babel-core/register --recursive --require test/beforeRunning.js"
  },
  "dependencies": {
    "aws-sdk": "^2.5.3"
  },
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-core": "^6.14.0",
    "babel-preset-es2015": "^6.14.0",
    "babel-preset-stage-0": "^6.5.0",
    "chai": "^3.5.0",
    "mocha": "^3.0.2",
    "sinon": "^1.17.5"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
